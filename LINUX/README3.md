```
/etc/os-release         # Bu fayl Linux distronun versiyası və adı ilə bağlı məlumatları saxlayır.
cat /etc/os-release     # cat əmri faylı ekrana çap edir
source /etc/os-release  # source əmri faylı oxuyur və içindəki dəyişənləri terminal mühitinə yükləyir.
echo $ID                # ubuntu
echo $VERSION_ID        # 22.04 
echo $NAME              # Ubuntu
```
